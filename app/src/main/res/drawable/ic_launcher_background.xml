<?xml version="1.0" encoding="utf-8"?>
<!--
    Adaptive-icon background layer. The FoodFit Scan mark is drawn on white, so the
    background is a flat white plate: the launcher's mask (circle, squircle, teardrop…)
    shapes it, and the foreground artwork never sits on a competing colour.
-->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="108dp"
    android:height="108dp"
    android:viewportWidth="108"
    android:viewportHeight="108">
    <path
        android:fillColor="#FFFFFFFF"
        android:pathData="M0,0h108v108h-108z" />
</vector>
